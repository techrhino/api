{
  "kind": "collectionType",
  "collectionName": "clients",
  "info": {
    "name": "client",
    "description": ""
  },
  "options": {
    "increments": true,
    "timestamps": true,
    "draftAndPublish": true
  },
  "attributes": {
    "name": {
      "type": "string",
      "required": true
    },
    "projects": {
      "collection": "project",
      "via": "customers"
    },
    "slug": {
      "type": "uid",
      "required": true
    },
    "logo": {
      "model": "file",
      "via": "related",
      "allowedTypes": [
        "images"
      ],
      "plugin": "upload",
      "required": false
    },
    "contacts": {
      "via": "client",
      "collection": "contact"
    }
  }
}
